# UnixTimestampExplorer

A static, browser-only Unix timestamp and date converter with seconds and
milliseconds, local date formatting, relative time, current-time input, and
copyable results.

Open `index.html` directly or serve this directory with a static file server.
