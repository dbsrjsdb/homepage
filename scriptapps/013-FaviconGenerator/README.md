# FaviconGenerator

A static, browser-only favicon builder that turns an emoji or uploaded image
into 16, 32, 48, and 180 pixel PNG assets.

Open `index.html` directly or serve this directory with a static file server.
Choose a source, adjust the corner radius, preview the assets, and download
the generated PNGs.
