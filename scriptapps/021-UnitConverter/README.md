# UnitConverter

A static, browser-only unit converter for length, mass, temperature, speed,
area, volume, and data measurements.

Open `index.html` directly or serve this directory with a static file server.
All conversions run locally without a backend.
