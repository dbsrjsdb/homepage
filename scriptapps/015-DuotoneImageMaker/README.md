# DuotoneImageMaker

A static, browser-only image duotone utility with custom shadow/highlight
colors, contrast, inversion, preview, and PNG export.

Open `index.html` directly or serve this directory with a static file server.
Images are processed locally and are not uploaded.
