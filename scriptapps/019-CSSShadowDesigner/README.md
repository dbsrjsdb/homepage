# CSSShadowDesigner

A static, browser-only CSS shadow designer for box and text shadows.

Open `index.html` directly or serve this directory with a static file server.
Tune offsets, blur, spread, opacity, colors, and target type, then copy the
generated CSS declaration.
