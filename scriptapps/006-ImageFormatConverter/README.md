# ImageFormatConverter

A static, browser-only utility for converting images between PNG, JPEG, and WebP.

Open `index.html` directly or serve this directory with a static file server.
No build step or backend is required; images stay in the browser.

PNG preserves transparency. JPEG and WebP provide a quality control from 10%
to 100%.
