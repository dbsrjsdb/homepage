# SpriteSheetCutter

A static, browser-only sprite sheet cutter with configurable rows and columns,
individual frame previews, selection, and PNG export.

Open `index.html` directly or serve this directory with a static file server.
Sprite sheets are processed locally and are not uploaded.
