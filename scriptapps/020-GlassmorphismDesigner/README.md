# GlassmorphismDesigner

A static, browser-only glassmorphism panel designer with live blur, opacity,
corner radius, border, and background controls.

Open `index.html` directly or serve this directory with a static file server.
Copy the generated CSS declaration for use in another project.
