# ImageColorReplacer

A static, browser-only image color replacement utility. Pick a source color
directly from an image, set a replacement color and tolerance, then download a
PNG result.

Open `index.html` directly or serve this directory with a static file server.
Images are processed locally and are not uploaded.
