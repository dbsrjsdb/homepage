# SVGPatternGenerator

A static, browser-only geometric SVG pattern builder with grid, dots, diagonal
stripe, and diamond styles.

Open `index.html` directly or serve this directory with a static file server.
Tune colors, tile size, line weight, rotation, and opacity, then copy or
download the generated SVG.
