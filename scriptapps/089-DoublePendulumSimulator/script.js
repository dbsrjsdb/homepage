const canvas = document.querySelector("#canvas"); const context = canvas.getContext("2d"); const controls = { angleOne: document.querySelector("#angle-one"), angleTwo: document.querySelector("#angle-two"), gravity: document.querySelector("#gravity") }; const trails = document.querySelector("#trails"); const status = document.querySelector("#status"); const state = { theta1: 0, theta2: 0, velocity1: 0, velocity2: 0 }; let paused = false; const length1 = 145; const length2 = 145; const mass1 = 1; const mass2 = 1;
function reset() { state.theta1 = Number(controls.angleOne.value) * Math.PI / 180; state.theta2 = Number(controls.angleTwo.value) * Math.PI / 180; state.velocity1 = 0; state.velocity2 = 0; paused = false; document.querySelector("#pause").textContent = "Pause"; document.querySelector("#state").textContent = "Running"; status.textContent = "Pendulum reset with the current starting angles."; }
function step() { if (paused) return; const g = Number(controls.gravity.value) / 10 * 9.8; const { theta1, theta2, velocity1: w1, velocity2: w2 } = state; const delta = theta1 - theta2; const den1 = (mass1 + mass2) * length1 - mass2 * length1 * Math.cos(delta) ** 2; const a1 = (mass2 * length1 * w1 * w1 * Math.sin(delta) * Math.cos(delta) + mass2 * g * Math.sin(theta2) * Math.cos(delta) + mass2 * length2 * w2 * w2 * Math.sin(delta) - (mass1 + mass2) * g * Math.sin(theta1)) / den1; const den2 = (length2 / length1) * den1; const a2 = (-mass2 * length2 * w2 * w2 * Math.sin(delta) * Math.cos(delta) + (mass1 + mass2) * (g * Math.sin(theta1) * Math.cos(delta) - length1 * w1 * w1 * Math.sin(delta) - g * Math.sin(theta2))) / den2; state.velocity1 += a1 * .012; state.velocity2 += a2 * .012; state.theta1 += state.velocity1 * .012; state.theta2 += state.velocity2 * .012; }
function draw() { context.fillStyle = trails.checked ? "#080b10cc" : "#080b10"; context.fillRect(0, 0, canvas.width, canvas.height); const origin = { x: canvas.width / 2, y: 100 }; const first = { x: origin.x + Math.sin(state.theta1) * length1, y: origin.y + Math.cos(state.theta1) * length1 }; const second = { x: first.x + Math.sin(state.theta2) * length2, y: first.y + Math.cos(state.theta2) * length2 }; context.strokeStyle = "#f7d36b"; context.lineWidth = 3; context.beginPath(); context.moveTo(origin.x, origin.y); context.lineTo(first.x, first.y); context.lineTo(second.x, second.y); context.stroke(); context.fillStyle = "#f7d36b"; context.beginPath(); context.arc(origin.x, origin.y, 7, 0, Math.PI * 2); context.fill(); context.fillStyle = "#ff9bbc"; context.shadowColor = "#ff9bbc"; context.shadowBlur = 20; context.beginPath(); context.arc(first.x, first.y, 12, 0, Math.PI * 2); context.fill(); context.fillStyle = "#91d9ff"; context.beginPath(); context.arc(second.x, second.y, 9, 0, Math.PI * 2); context.fill(); context.shadowBlur = 0; for (let index = 0; index < 3; index++) step(); requestAnimationFrame(draw); }
function updateLabels() { document.querySelector("#angle-one-value").textContent = `${Number(controls.angleOne.value) < 0 ? "−" : ""}${Math.abs(controls.angleOne.value)}°`; document.querySelector("#angle-two-value").textContent = `${Number(controls.angleTwo.value) < 0 ? "−" : ""}${Math.abs(controls.angleTwo.value)}°`; document.querySelector("#gravity-value").textContent = `${(Number(controls.gravity.value) / 10).toFixed(1)}×`; }
Object.values(controls).forEach(input => input.addEventListener("input", () => { updateLabels(); reset(); })); trails.addEventListener("change", () => { status.textContent = trails.checked ? "Motion trail enabled." : "Motion trail hidden."; }); document.querySelector("#reset").addEventListener("click", reset); document.querySelector("#pause").addEventListener("click", () => { paused = !paused; document.querySelector("#pause").textContent = paused ? "Resume" : "Pause"; document.querySelector("#state").textContent = paused ? "Paused" : "Running"; status.textContent = paused ? "Pendulum paused." : "Pendulum resumed."; }); updateLabels(); reset(); draw();
