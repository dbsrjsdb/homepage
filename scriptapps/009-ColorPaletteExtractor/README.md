# ColorPaletteExtractor

A static, browser-only utility that samples an image and extracts its dominant
colors as a palette.

Open `index.html` directly or serve this directory with a static file server.
Images are analyzed locally and are not uploaded. Choose 3–10 colors, copy the
palette as CSS, or download it as a text file.
