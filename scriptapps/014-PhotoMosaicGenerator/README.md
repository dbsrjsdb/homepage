# PhotoMosaicGenerator

A static, browser-only photo mosaic utility that reconstructs an uploaded image
from average-color tiles or repeated emoji.

Open `index.html` directly or serve this directory with a static file server.
Adjust tile density and gaps, choose a tile style, and export a PNG.
