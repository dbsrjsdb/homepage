# GradientStudio

A static, browser-only CSS gradient builder for linear, radial, and conic
gradients.

Open `index.html` directly or serve this directory with a static file server.
Adjust color stops and angles, copy the generated CSS, or download it as a
stylesheet snippet.
