# CSSBlobGenerator

A static, browser-only organic blob generator with controls for point count,
irregularity, rotation, and color.

Open `index.html` directly or serve this directory with a static file server.
Copy the generated CSS border-radius or download the SVG shape.
