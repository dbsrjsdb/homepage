# ASCIIArtGenerator

A static, browser-only image-to-ASCII converter with adjustable character
width, character sets, contrast, inversion, clipboard copy, and text export.

Open `index.html` directly or serve this directory with a static file server.
Images are processed locally and are not uploaded. Input images are limited to
20 MB.
