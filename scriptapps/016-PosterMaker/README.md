# PosterMaker

A static, browser-only poster maker with image backgrounds, headline wrapping,
typography controls, color/overlay settings, and PNG export.

Open `index.html` directly or serve this directory with a static file server.
Images are processed locally and are not uploaded.
