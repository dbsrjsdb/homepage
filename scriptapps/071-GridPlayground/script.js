const ids = ["items", "columns", "rows", "gap", "justify", "align"]; const preview = document.querySelector("#grid-preview"); const output = document.querySelector("#css-output"); const summary = document.querySelector("#summary"); const status = document.querySelector("#status");
function render() { const items = Number(document.querySelector("#items").value); const columns = Number(document.querySelector("#columns").value); const rows = Number(document.querySelector("#rows").value); const gap = Number(document.querySelector("#gap").value); const justify = document.querySelector("#justify").value; const align = document.querySelector("#align").value; document.querySelector("#item-value").textContent = items; document.querySelector("#column-value").textContent = columns; document.querySelector("#row-value").textContent = rows; document.querySelector("#gap-value").textContent = `${gap}px`; preview.style.gridTemplateColumns = `repeat(${columns}, 1fr)`; preview.style.gridTemplateRows = `repeat(${rows}, 1fr)`; preview.style.gap = `${gap}px`; preview.style.justifyItems = justify; preview.style.alignItems = align; preview.innerHTML = Array.from({ length: items }, (_, index) => `<span class="box">${index + 1}</span>`).join(""); const css = `display: grid;\ngrid-template-columns: repeat(${columns}, 1fr);\ngrid-template-rows: repeat(${rows}, 1fr);\ngap: ${gap}px;\njustify-items: ${justify};\nalign-items: ${align};`; output.textContent = css; summary.textContent = `${columns} × ${rows} · ${items} items`; }
ids.forEach(id => document.querySelector(`#${id}`).addEventListener("input", () => { render(); status.textContent = "Grid preview updated locally."; status.className = "status good"; })); document.querySelector("#copy").addEventListener("click", async () => { try { await navigator.clipboard.writeText(output.textContent); status.textContent = "CSS copied to the clipboard."; status.className = "status good"; } catch { status.textContent = "Clipboard access is unavailable; select the CSS to copy it."; } }); document.querySelector("#reset").addEventListener("click", () => { document.querySelector("#items").value = 9; document.querySelector("#columns").value = 3; document.querySelector("#rows").value = 3; document.querySelector("#gap").value = 12; document.querySelector("#justify").value = "stretch"; document.querySelector("#align").value = "stretch"; render(); status.textContent = "Defaults restored."; status.className = "status"; }); render();
